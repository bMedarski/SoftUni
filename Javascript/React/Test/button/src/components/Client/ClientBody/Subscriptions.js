import React from 'react';
import Subscription from './Subscription';

const Subscriptions = ({
    subscriptions, validations
}) => (
        <div>
            <ul>
               <Subscription subscription={'TR Acurracy MiFIR'}/> 
            </ul>
        </div>
    )
export default Subscriptions